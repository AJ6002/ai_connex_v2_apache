# Copy this file to .env and fill in your key.
# If GEMINI_API_KEY is left unset, the backend automatically falls back to a
# keyword-based simulator (see extraction.py) so you can test the full
# pipeline without a live key.
GEMINI_API_KEY=

# Port the Flask server listens on
PORT=5000
