# Base URL for the AI-ConneX FastAPI application layer.
# Every network call in the app resolves its host from this single value.
VITE_API_BASE=http://localhost:8000

# When "true", the app starts the MSW mock service worker and serves all
# capability endpoints from local fixtures instead of a real backend.
VITE_USE_MOCKS=true
